name: MLLibrary
version: 0.1.0.0
dependencies:
  - base >= 4.7 && < 5
  - hmatrix
  - random

executables:
  MLLibrary-exe:
    main: Main.hs
    source-dirs: .
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
